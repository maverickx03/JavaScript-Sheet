const webTechs = [
    'HTML',
    'CSS',
    'JavaScript',
    'React',
    'Redux',
    'Node',
    'MongoDB'
    ]

    const arr=[];
    const arr2 = [[]];
    for( const i of webTechs){

        arr.push(i,i.length);
        arr2.push(arr);


    }
    console.log(arr2);   // ouput not Obtained      